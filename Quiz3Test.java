import java.util.Scanner; // Imports the Scanner object used for user prompts
public class Quiz3Test
{
	public static void main(String[] args)
	{
		int[] scores = {1, 20, 30, 40, 50};
		int[] t = {1, 2, 3, 4};
		System.out.println ("t.length = " + t.length);
	
		int k = java.util.Arrays.binarySearch (scores, 20);
		System.out.println (" k = " + k);
		
		System.out.println ("Math.ceil(9.2) = " + Math.ceil(9.2));
		
		System.out.println("(Math.max(23.7, 95.6) = " + (Math.max(23.7, 95.6)));
	}
		
}
